# Rust
rust-core/target/

# Node test-only devDependencies (see package.json — not used by the
# deployed app, which loads @solana/web3.js from a CDN instead)
node_modules/
package-lock.json

# WASM build output — was ignored when it was purely a local build
# artifact; now committed automatically by ci.yml's wasm-build job so
# the deployed site actually serves the compiled binary, not just the
# JS fallback. Deliberately NOT ignored anymore.

# OS
.DS_Store
